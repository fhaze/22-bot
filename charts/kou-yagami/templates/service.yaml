apiVersion: v1
kind: Service
metadata:
  name: {{ include "22-bot.fullname" . }}
  labels:
    {{- include "22-bot.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "22-bot.selectorLabels" . | nindent 4 }}
