apiVersion: v1
kind: Service
metadata:
  name: {{ include "kou-yagami.fullname" . }}
  labels:
    {{- include "kou-yagami.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kou-yagami.selectorLabels" . | nindent 4 }}
