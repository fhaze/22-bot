apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "kou-yagami.fullname" . }}-test-connection"
  labels:
    {{- include "kou-yagami.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "kou-yagami.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
