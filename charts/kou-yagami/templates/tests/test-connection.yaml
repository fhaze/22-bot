apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "22-bot.fullname" . }}-test-connection"
  labels:
    {{- include "22-bot.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "22-bot.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
